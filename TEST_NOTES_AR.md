# ملاحظات فحص World Dominion v0.7

تم قبل التغليف:

- فحص صياغة Node.js للسيرفر بواسطة `node --check`.
- التأكد أن نسخة السيرفر داخل مشروع Flutter مطابقة لنسخة السيرفر المنفصلة.
- فحص بنيوي للأقواس في `lib/main.dart`.
- التحقق من صحة ملفات YAML و JSON الأساسية.
- تجهيز GitHub Actions لتشغيل `flutter analyze` ثم بناء APK Release تلقائياً.

## ما لم يُنفّذ داخل بيئة ChatGPT

لا يتوفر Flutter/Android SDK كامل داخل بيئة التنفيذ الحالية، لذلك لم يتم تنفيذ `flutter analyze` أو `flutter build apk` محلياً هنا. ملف GitHub Actions الموجود في المشروع يقوم بهذه الخطوة على runner مجهز بالـ SDK عند رفع المشروع إلى GitHub.


## اختبارات v1.2 Combat

- يجب بقاء اختبارات v1.1 السابقة وعدم حذفها.
- اختبار `frontlineProgressDelta` يتحقق أن التقدم تدريجي ومحدود.
- اختبار Snapshot يتحقق أن حالة الجبهة تُحفظ وتُستعاد، وأن حفظاً قديماً بلا `battles` يقرأ بقائمة فارغة.
- السيرفر يحتوي `combat_rules_test.js` ويشغله `npm run check` مع `seed_test.js`.
- قبل اعتبار APK جاهزاً يجب أن يمر `flutter analyze` و`flutter test` و`flutter build apk --release` في CI/بيئة Flutter حقيقية.

## مراجعة طرفية إضافية — 1.2 Combat
- لا تُنشأ جبهة جديدة إذا فشل فحص نقاط القيادة أو الموارد.
- الانسحاب الدفاعي يقبل ممرًا بريًا صديقًا أو ممرًا بحريًا صالحًا عند توفر الأسطول والموانئ.
- إرسال `armyIds` في Multiplayer يفرق بين: عدم تحديد القائمة (استخدام الجيوش المتاحة تلقائيًا) وقائمة فارغة مقصودة (القوات الوطنية فقط).
